name: Deploy production

on:
  workflow_dispatch:

concurrency:
  group: azulyoro-production
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Explain server-side deploy
        run: |
          echo "Production deploy is performed by the VPS timer azulyoro-autodeploy.timer."
          echo "This manual workflow does not require SSH secrets and does not deploy directly."
